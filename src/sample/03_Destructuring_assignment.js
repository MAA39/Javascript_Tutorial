/**
 * 分割代入
 */

const myProfile = {
  name: "田中",
  age: 19
};

const message = `名前は${myProfile.name}、年齢は${myProfile.age}歳です。`;
console.log(message);

//分割代入　＝　オブジェクトから指定のプロパティを抜き出して自由に使えるようになるもの
//課題：
